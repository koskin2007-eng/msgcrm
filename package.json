{
  "name": "ai-manager",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build -w @ai-manager/shared && npm run build -w @ai-manager/api && npm run build -w @ai-manager/web",
    "typecheck": "npm run typecheck -w @ai-manager/shared && npm run typecheck -w @ai-manager/api && npm run typecheck -w @ai-manager/web",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "overrides": {
    "postcss": "^8.5.14"
  }
}
