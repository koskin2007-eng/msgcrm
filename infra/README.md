# Infrastructure

This folder contains templates for local development and deployment.

The production server currently keeps runtime compose files in:

```text
/opt/ai-manager/infra
```

Application source can be deployed to:

```text
/opt/ai-manager/app
```

Do not commit production `.env` files or real secrets.
